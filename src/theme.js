import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#000000',
    },
    background: {
      default: '#f2f2f2',
      white: '#ffffff',
    }
  }
})

export default theme